system:
  video_sources:
    - "data/raw_videos/gate_side1.mp4"
    - "data/raw_videos/gate_side2.mp4"
  video_path: "data/raw_videos/gate_side2.mp4"  # 하위 호환용 유지
  log_file: "outputs/gate_log.csv"
  temp_frame_dir: "temp_frames"

model:
  yolo_path: "outputs/yolo_container_ocr/weights/best.pt"
  yolo_default: "models/yolo11n.pt"
  conf_threshold: 0.5
  ocr_model: "Qwen/Qwen3-VL-2B-Instruct"

parameters:
  cooldown_frames: 150
  perspective_intensity: 0.0
